url: https://janickweberpals.github.io/encore.analytics/
template:
  bootstrap: 5

navbar:
  structure:
    left: [intro, reference, articles, tutorials, news, reports]
    right: [search]
  components:
   reports:
    text: Reports
    menu:
    - text: Coverage report
      href: coverage.html # covr::report(covr::package_coverage(), file = 'docs/coverage.html', browse = FALSE)
